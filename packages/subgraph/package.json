{
  "name": "just-feedback",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph build; graph deploy --node https://api.thegraph.com/deploy/ sturmenta/just-feedback",
    "create-local": "graph create --node http://localhost:8020/ sturmenta/just-feedback",
    "remove-local": "graph remove --node http://localhost:8020/ sturmenta/just-feedback",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sturmenta/just-feedback"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
