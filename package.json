{
  "name": "@my-app/monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "yarn workspace @my-app/contracts deploy",
    "ios": "yarn workspace @my-app/rn-app ios"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
